//Replicator {
  akka {
    // event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
    loglevel = "DEBUG"
    actor {
      debug {
        // receive = on
        // autoreceive = on
        // lifecycle = on
        // fsm = on
      }
    }
  }

  pinned-dispatcher {
    type = PinnedDispatcher
  }
// }
