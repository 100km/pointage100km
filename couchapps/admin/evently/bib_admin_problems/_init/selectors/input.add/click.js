function() {
  var site_id = $(this).attr('data-site_id');
  var lap = $(this).attr('data-lap');
  var bib = $(this).attr('data-bib');

  // TODO: call database and insert a time that the average between the previous and the next time
  // NOTE: we ma need to compte the average in the error detection and pass it here through the button
}
